print ('радиус окружности в сантиметрах')
r = int
r = input()

if int(r) >= 0:

    c = int
    c =  2*int(r)*3,14
    print(str(c) + 'длина в сантиметрах')
    cm = in /100
    print(str(cm) + 'длина окружности в метрах')

    so = int
    so = 3,14*int(r)**2
    print(str(so) + 'площадь в метрах')
    som = int
    som = int(som) / 100

    a = int
    a = (2*int(r))**0,5
    

 